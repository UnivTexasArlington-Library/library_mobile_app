/// <reference types="react" />
import { DecreasingDot, DotConfig } from './interface';
export interface CarouselDotsProps {
    length: number;
    currentIndex: number;
    maxIndicators: number;
    activeIndicatorConfig: DotConfig;
    inactiveIndicatorConfig: DotConfig;
    decreasingDots: DecreasingDot[];
    verticalOrientation?: boolean;
    interpolateOpacityAndColor?: boolean;
}
declare const CarouselDots: ({ length, currentIndex, maxIndicators, activeIndicatorConfig, inactiveIndicatorConfig, decreasingDots, verticalOrientation, interpolateOpacityAndColor, }: CarouselDotsProps) => JSX.Element;
export default CarouselDots;

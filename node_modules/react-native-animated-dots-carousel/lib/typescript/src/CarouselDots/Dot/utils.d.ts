import { DecreasingDot, DotConfig } from '../interface';
interface GetDotStyle {
    index: number;
    currentIndex: number;
    maxIndicators: number;
    activeIndicatorConfig: DotConfig;
    inactiveIndicatorConfig: DotConfig;
    decreasingDots: DecreasingDot[];
    indicatorState: number;
}
export declare const getDotStyle: ({ index, currentIndex, maxIndicators, activeIndicatorConfig, inactiveIndicatorConfig, decreasingDots, indicatorState, }: GetDotStyle) => DotConfig;
export {};
